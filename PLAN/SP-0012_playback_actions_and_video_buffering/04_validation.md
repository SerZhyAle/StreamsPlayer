# Phase 04 — Validation

1. [Done] Run release build and full Core tests.
2. [Partial] Open two reachable video/RTSP streams through the independent-window command; observe independent windows, buffering text/progress, and stable playback. This awaits a user-run scenario because the existing user-owned Debug process remains active.

Static check: evidence records expected/actual and does not claim a live delay that was not observed.
