# Phase 02 — Compact layout

**Consumes:** Existing localized controls and action handlers.

1. [Done] Place product title and localized subtitle side by side; place all existing header controls in one right-aligned horizontal action group.
2. [Done] Replace the stacked search/filter layout with a labeled single controls line without changing names, handlers, tooltips, or automation names.

Static check: XAML has one header action container and one controls container; every selector still has a visible label.
