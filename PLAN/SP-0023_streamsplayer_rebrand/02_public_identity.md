# Phase 02 — Public identity

**Status:** Approved

1. Update current English and Ukrainian public surfaces to the exact visible brand `STREAMS Player`; update Russian public surfaces and localized UI labels to `Трансляции`. Static check: user-facing localized resources and public pages contain the required brand.
2. Update current repository/Pages URLs, MSIX display metadata, winget templates, CI/release documentation, and issue templates to the StreamsPlayer identity without publishing any artifact. Static check: active public links resolve to the plural repository/path form.
