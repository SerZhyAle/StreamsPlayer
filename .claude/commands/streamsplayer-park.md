---
description: Capture an out-of-scope StreamsPlayer finding as a Draft stub, then resume the current task.
argument-hint: [task or ticket]
---
Follow the procedure in `.agents/skills/streamsplayer-park/SKILL.md` exactly.

Ground rules: obey `AGENTS.md` and the applicable `docs/agent/` method document, work at the lowest sufficient complexity rung, keep `StreamsPlayer.Core` platform-neutral, preserve explicit catalog refresh and the MANUAL/IMPORTED merge contract, and record command checks as `expected: ... | actual: ...`.

Task / arguments: $ARGUMENTS
