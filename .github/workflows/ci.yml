name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.props', '**/*.sln') }}
          restore-keys: ${{ runner.os }}-nuget-
      - name: Restore
        run: dotnet restore StreamPlayer.sln
      - name: Build
        run: dotnet build StreamPlayer.sln -c Release --no-restore
      - name: Test
        run: dotnet test StreamPlayer.sln -c Release --no-build --verbosity normal
