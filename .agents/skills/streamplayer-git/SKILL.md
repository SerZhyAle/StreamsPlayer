---
name: streamplayer-git
description: Inspect or organize StreamPlayer Git work safely, including branch choice, staging groups, and concise commit guidance.
---
For StreamPlayer, follow `AGENTS.md`, the applicable `docs/agent/` method document, and the task’s lowest sufficient complexity rung. Keep reports concise and record command checks as `expected: ... | actual: ...`.

1. Inspect status and branch before advising on staging.
2. Keep logical changes separate; never use broad staging blindly.
3. Do not commit, push, rebase, tag, publish, or change shared history unless the user explicitly asks.
4. Respect the AGENTS publication boundary and report the proposed Git action concisely.

