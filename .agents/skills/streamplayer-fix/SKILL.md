---
name: streamplayer-fix
description: Fix a narrow, understood StreamPlayer behaviour bug with a bounded blast radius and focused validation. Do not use for new design work.
---
For StreamPlayer, follow `AGENTS.md`, the applicable `docs/agent/` method document, and the task’s lowest sufficient complexity rung. Keep reports concise and record command checks as `expected: ... | actual: ...`.

1. Cite the observed symptom, current path, and root cause before editing.
2. Fix the bug, not the surrounding neighbourhood. Add or adjust a focused test when it is practical.
3. Run the narrowest meaningful formatter, build, or test check.
4. Report root cause, fix, and validation in one line each.

