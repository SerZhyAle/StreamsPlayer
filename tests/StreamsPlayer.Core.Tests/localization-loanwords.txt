# SP-0034: keys whose correct translation in a given language IS the English word.
#
# Format: <dictionary code>:<resource key> # reason
#
# Read by LocalizationParityTests.NoTranslationIsLeftInEnglish. An entry here says "this string is
# deliberately identical to English in this language", which is a normal outcome for a loanword.
#
# Do NOT use this file to wave through a string nobody translated. And never pick a worse synonym just
# to avoid adding an entry - that is the failure this file exists to prevent. Every line needs a reason
# a reviewer can check.
#
# EveryLoanwordExceptionIsStillNeeded deletes the slack automatically: if the translation later stops
# matching English, or the key or language goes away, that test fails until the stale line is removed.

# SP-0061 note on the rubric labels (Topic*): music-genre names are the loanword case this file was
# written for. Where a language genuinely writes the English word - "Rock" in German, "Reggae" in
# French - the entry below says so. Where it does not, the label is translated and no entry exists.

# --- German ---
de:TopicTalk        # German radio bills a spoken-word station as "Talk".
de:TopicComedy      # "Comedy" is the established German genre name; "Komödie" means a stage or film play.
de:TopicLifestyle   # "Lifestyle" is the ordinary German word for this genre.
de:TopicShopping    # "Shopping" is German; a "Shoppingsender" is what such a channel is called.
de:TopicTest        # "Test" is German.
de:TopicPop         # Genre name, written the same way.
de:TopicRock        # Genre name, written the same way.
de:TopicMetal       # Genre name, written the same way.
de:TopicChillout    # "Chillout" is the German genre name too.
de:TopicRnbSoul     # Both genre names are English in German; only the ampersand joins them.
de:TopicJazzBlues   # Same: "Jazz" and "Blues" are the German words.
de:TopicCountryFolk # "Country" and "Folk" are the German genre names.
de:TopicReggae      # Genre name, written the same way.
de:TopicLatin       # German radio labels the genre "Latin"; "lateinisch" means the language.
de:TopicOldies      # "Oldies" is the standard German radio format name.

de:LiveLabel        # "Live" is the standard German broadcast term.
de:AudioOption      # "Audio" is German.
de:KindAudio        # Same word, upper-cased badge.
de:AudioTracks      # "Audio" heads the German label too.
de:VideoOption      # "Video" is German.
de:KindVideo        # Same word, upper-cased badge.
de:SortName         # "Name" is German.
de:FieldFormat      # "Format" is German.
de:FieldBitrate     # "Bitrate" is the established German term.
de:MediaKindAuto    # "Auto" is the usual German abbreviation for automatic.
de:VersionLabel     # "Version" is German.
de:Website          # "Website" is the common German usage.
de:FieldTitle       # "Name" is German.
de:FrameRateValue   # "fps" is what German media software prints for frames per second.

# --- Italian ---
it:LiveLabel        # "Live" is standard Italian broadcast usage.
it:AudioOption      # "Audio" is Italian.
it:KindAudio        # Same word, upper-cased badge.
it:AudioTracks      # "Audio" heads the Italian label too.
it:VideoOption      # "Video" is Italian.
it:KindVideo        # Same word, upper-cased badge.
it:Media            # "Media" is Italian - it is a Latin plural to begin with.
it:FieldBitrate     # "Bitrate" is the established Italian term.
it:MediaKindAuto    # "Auto" is the usual Italian abbreviation for automatic.
it:Buffering        # "Buffering" is what Italian media software says.
it:BufferingProgress # Same term with the percentage placeholder.
it:Privacy          # "Privacy" is the ordinary Italian word for this.
it:AboutValueNo     # "No" is the Italian word for no.
it:FrameRateValue   # "fps" is what Italian media software prints for frames per second.
it:TopicTalk        # Italian radio bills a spoken-word station as "talk".
it:TopicLifestyle   # "Lifestyle" is the usual Italian term for this genre.
it:TopicShopping    # "Shopping" is Italian.
it:TopicWebcam      # "Webcam" is Italian.
it:TopicTest        # "Test" is Italian.
it:TopicPop         # Genre name, written the same way.
it:TopicRock        # Genre name, written the same way.
it:TopicMetal       # Genre name, written the same way.
it:TopicChillout    # "Chillout" is the Italian genre name too.
it:TopicHipHop      # Genre name, written the same way.
it:TopicReggae      # Genre name, written the same way.

# --- Portuguese ---
pt:FieldBitrate     # "Bitrate" is what Brazilian media software labels this field; "taxa de bits" reads as a spec sheet.
pt:FrameRateValue   # "fps" is what Portuguese media software prints for frames per second.
pt:TopicPop         # Genre name, written the same way.
pt:TopicRock        # Genre name, written the same way.
pt:TopicMetal       # Genre name, written the same way.
pt:TopicChillout    # "Chillout" is the Brazilian genre name too.
pt:TopicHipHop      # Genre name, written the same way.
pt:TopicReggae      # Genre name, written the same way.

# --- Chinese ---
zh:FrameRateValue   # "fps" is written in Latin script in Chinese media software, like "kbps".

# --- Spanish ---
es:AudioOption      # "Audio" is Spanish; the filter option keeps it.
es:KindAudio        # Same word, upper-cased badge.
es:AudioTracks      # "Audio" heads the Spanish label too, matching "pista de audio" in its tooltip.
es:MediaKindAuto    # "Auto" is the usual Spanish abbreviation for automatic.
es:AboutValueNo     # "No" is the Spanish word for no.
es:FrameRateValue   # "fps" is what Spanish media software prints for frames per second.
es:TopicGeneral     # "General" is the Spanish word, spelled identically.
es:TopicPop         # Genre name, written the same way.
es:TopicRock        # Genre name, written the same way.
es:TopicMetal       # Genre name, written the same way.
es:TopicChillout    # "Chillout" is the Spanish genre name too.
es:TopicHipHop      # Genre name, written the same way.
es:TopicReggae      # Genre name, written the same way.

# --- French ---
fr:AudioOption      # "Audio" is French; the filter option keeps it.
fr:KindAudio        # Same word, upper-cased badge.
fr:AudioTracks      # "Audio" heads the French label too, matching "piste audio" in its tooltip.
fr:MediaKindAuto    # "Auto" is the usual French abbreviation for automatique.
fr:VersionLabel     # "Version" is French.
fr:FieldType        # "Type" is French.
fr:FieldFormat      # "Format" is French.
fr:SleepTimerMinutes # "minutes" is the same word in French; only the placeholder precedes it.
fr:CollectionLabel  # "Collection" is French and is the glossary term for fr.
fr:CollectionManageTitle # Plural of the same French word.
fr:FieldOrigin      # "Source" is French.
fr:FieldCollections # Plural of the French glossary term for collection.
fr:TopicShopping    # "Shopping" is French.
fr:TopicTest        # "Test" is French.
fr:TopicPop         # Genre name, written the same way.
fr:TopicRock        # Genre name, written the same way.
fr:TopicChillout    # "Chillout" is the French genre name too.
fr:TopicHipHop      # Genre name, written the same way.
fr:TopicReggae      # Genre name, written the same way.

# --- Hindi ---
hi:FrameRateValue   # "fps" is written in Latin script in Hindi media software, like "kbps".

# --- Bengali ---
bn:FrameRateValue   # "fps" is written in Latin script in Bengali media software, like "kbps".

# --- Urdu ---
ur:FrameRateValue   # "fps" is written in Latin script in Urdu media software, like "kbps".
