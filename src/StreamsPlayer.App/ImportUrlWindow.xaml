<Window x:Class="StreamsPlayer.App.ImportUrlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource ImportFromUrlTitle}" Height="180" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackground}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource ImportFromUrlPrompt}" TextWrapping="Wrap" Margin="0,0,0,8" />
        <TextBox x:Name="UrlBox" Grid.Row="1" Padding="6,4" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="ConfirmButton" Content="{DynamicResource ImportListPlain}" Click="Confirm_Click"
                    IsDefault="True" Padding="16,6" Margin="0,0,8,0" />
            <Button Content="{DynamicResource Cancel}" IsCancel="True" Padding="14,6" />
        </StackPanel>
    </Grid>
</Window>
